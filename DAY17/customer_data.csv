import pandas as pd
import random

# Create sample dataset
data = {
    "Customer_ID": list(range(1, 31)),
    "Age": [random.randint(18, 60) for _ in range(30)],
    "Spending": [random.randint(1000, 10000) for _ in range(30)],
    "Visits": [random.randint(1, 20) for _ in range(30)]
}

df = pd.DataFrame(data)

# Save CSV correctly
df.to_csv("customer_data.csv", index=False)

print("CSV Created Successfully")
print(df.head())
